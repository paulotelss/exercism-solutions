#if !defined(REVERSE_STRING_H)
#define REVERSE_STRING_H

#include <string>

namespace reverse_string {
    // Retorna uma nova string invertida
    std::string reverse_string(const std::string& text);
}

#endif