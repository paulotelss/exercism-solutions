#pragma once

#include <vector>

namespace spiral_matrix {

	// Retorna uma matriz quadrada (vetor de vetores) de tamanho size x size
	std::vector<std::vector<int>> spiral_matrix(int size);

}  // namespace spiral_matrix